# Track job changes — composed from 3 skill(s) somebody else wrote.
#
# ORDER IS COMPOSITION ORDER, and it decides the credit line: `built_on` is
# derived from skill frontmatter in first-appearance order and can never be
# typed. Nothing about the credit on this page is a claim we made.
name: Track job changes
description: "Champions who moved, and the account they moved to."
category: watch-signals
version: 1.0.0
license: MIT
handler: llm

maintained_by:
  name: freegtm
  url: https://github.com/freegtmapps

say:
  - "who changed jobs this month?"
  - "did any champions move?"

skills:
  - track-contact-job-changes

# A SKILL THE CATALOGUE LISTS IS DELIBERATELY ABSENT, and the loader is why.
#
# `champion-move-detection` carries ..., COMPETITORS, CRM, DIGEST_CHANNEL, LINKEDIN_LOOKUP, MQL_OWNER, PRODUCT, REFRESH_WINDOW — uppercase tokens in the
# `{{NAME}}` convention some community skills use for values you are meant to
# replace before enabling. Nothing here can fill them, so they would reach the
# model as literal braces in a prompt somebody paid for.
#
# NOT A REFUSAL WE INVENTED: the substituter's own vocabulary is lowercase and
# closed, so these were invisible to it until a loader check went looking for
# any {{...}} no workspace value can fill. Editing his text to fit our
# substitution would make the credit line say `adapted` about a rewrite that was
# ours, so the skill goes in when upstream parameterises it.

# A SKILL THE CATALOGUE LISTS IS DELIBERATELY ABSENT, and the loader is why.
#
# `track-champion-job-changes` carries vars — no workspace value can fill it, so it would reach the model as literal braces in a — uppercase tokens in the
# `{{NAME}}` convention some community skills use for values you are meant to
# replace before enabling. Nothing here can fill them, so they would reach the
# model as literal braces in a prompt somebody paid for.
#
# NOT A REFUSAL WE INVENTED: the substituter's own vocabulary is lowercase and
# closed, so these were invisible to it until a loader check went looking for
# any {{...}} no workspace value can fill. Editing his text to fit our
# substitution would make the credit line say `adapted` about a rewrite that was
# ours, so the skill goes in when upstream parameterises it.

may:
  - read
  - write_lead
  - emit

# Declared at the TOP LEVEL because Requires.Tools is derived from it rather
# than hand-written — hand entry plus a load-time check would be two sources
# that agree until somebody edits one.
tools: [crm]

# Hand-written because it is not derivable: it says what this app degrades
# gracefully without, which is a maintainer's judgement. Say no once through
# set_reach and it stops asking.
requires:
  optional:
    tools: [linkedin, enrichment]

# Declarations that are INERT ON THE REGISTRY and live on the platform.
# Signals are the commercial product; nothing here raises these, and they stay
# so one bundle describes one app on both.
emits:
  - champion.moved

# NO {{placeholders}}. A workspace value named by a placeholder and left unset
# REFUSES composition, so a placeholder is a promise that the app cannot run
# until somebody has spoken. These apps do not need that: every declared
# workspace value reaches the prompt through the Context section for every app,
# whether or not its prose names the key.
instructions: |-
  Champions who moved, and the account they moved to.

  Work from what you can actually reach and what is on the lead. Name what you could not find rather than filling it in — a gap stated is worth more than a gap invented, because the invented one gets repeated out loud.

  Write what you learn to the lead with update_lead.
